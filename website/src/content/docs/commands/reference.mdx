---
title: All commands
description: Every DBRetina command at a glance, grouped by what it does.
---

import CommandGrid from '../../../components/CommandGrid.astro';

DBRetina is one binary with sixteen sub-commands. A run is usually `index` → `pairwise` → one analysis command.
Start typing to filter by name or description, or jump straight to any command below.

<CommandGrid />

New to the tool? The [Quickstart](/DBRetina/start/quickstart/) runs the core three end to end, and
[Command chaining](/DBRetina/reference/chaining/) shows how their inputs and outputs connect.
